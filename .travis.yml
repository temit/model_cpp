dist: bionic

language: cpp
compiler:
  - clang-7
  - g++-7.4.0

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test

script:
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build . -- VERBOSE=1 
  - ctest --verbose
  - bash <(curl -s https://codecov.io/bash)
